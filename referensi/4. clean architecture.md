Konsep Utama Clean Architecture

Clean Architecture membagi aplikasi menjadi:

Presentation Layer
↓
Domain Layer
↓
Data Layer

Rule penting:

Presentation → Domain → Data

Tidak boleh terbalik.

Tujuan Clean Architecture
1. UI Tidak Bergantung API

UI hanya tahu:

usecase
state
entity

UI tidak tahu:

endpoint
JSON
database
2. Mudah Ganti Backend

Hari ini:

Node.js

Besok:

Golang

Flutter tetap aman.

3. Mudah Testing

Karena business logic dipisah.

4. Modular Enterprise

Tim bisa kerja paralel:

tim inventory
tim order
tim payment
Struktur Besar
features/
└── order/
    ├── data/
    ├── domain/
    └── presentation/
1. PRESENTATION LAYER

Layer Flutter UI.

Berisi:

Page
Widget
Controller
State
Provider
Struktur Presentation
presentation/
├── pages/
├── widgets/
├── providers/
├── states/
├── controllers/
└── dialogs/
Tugas Presentation Layer
Bertanggung jawab:

✅ render UI
✅ handle click
✅ tampil loading
✅ tampil error
✅ listen realtime state

Tidak boleh:

❌ API call langsung
❌ business logic berat
❌ query database
❌ parsing JSON

Contoh Flow UI
Button Click
→ Provider
→ UseCase
→ Repository
→ API
Contoh Page
class OrderListPage extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final state = ref.watch(orderProvider);

    return Scaffold(
      body: state.when(
        data: (orders) => OrderListWidget(orders),
        loading: () => CircularProgressIndicator(),
        error: (e, _) => Text(e.toString()),
      ),
    );
  }
}
State Management

Rekomendasi:

Riverpod AsyncNotifier
Contoh Provider
final orderProvider =
    AsyncNotifierProvider<OrderNotifier, List<OrderEntity>>(
  OrderNotifier.new,
);
Contoh Notifier
class OrderNotifier extends AsyncNotifier<List<OrderEntity>> {
  @override
  Future<List<OrderEntity>> build() async {
    return ref.read(getOrdersUseCaseProvider).call();
  }
}
2. DOMAIN LAYER

Layer TERPENTING.

Pure business logic.

Tidak boleh import Flutter.

Struktur Domain
domain/
├── entities/
├── repositories/
├── usecases/
└── services/
Isi Domain Layer
Entities

Representasi object bisnis.

Contoh:

Order
Medicine
Stock
Payment
UseCases

Business logic utama.

Contoh:

CreateOrder
PayOrder
ReserveStock
TransferStock
Repository Contract

Interface.

Bukan implementasi.

Contoh Entity
class OrderEntity {
  final String id;
  final String orderNumber;
  final double total;

  OrderEntity({
    required this.id,
    required this.orderNumber,
    required this.total,
  });
}
Contoh Repository Contract
abstract class OrderRepository {
  Future<List<OrderEntity>> getOrders();

  Future<OrderEntity> createOrder(
    CreateOrderParams params,
  );
}
Contoh UseCase
class GetOrdersUseCase {
  final OrderRepository repository;

  GetOrdersUseCase(this.repository);

  Future<List<OrderEntity>> call() {
    return repository.getOrders();
  }
}
Kenapa UseCase Penting?

Karena semua business flow ada di sini.

Contoh:

Create Order
→ Validate Stock
→ Reserve Stock
→ Save Order
→ Broadcast Realtime
Domain Tidak Tahu

❌ Dio
❌ Flutter
❌ PostgreSQL
❌ JSON
❌ API

3. DATA LAYER

Layer implementasi.

Berisi:

API call
Local DB
WebSocket
DTO
Repository implementation
Struktur Data Layer
data/
├── datasources/
│   ├── remote/
│   └── local/
│
├── models/
├── repositories/
├── mappers/
└── services/
Tugas Data Layer

✅ hit API
✅ parse JSON
✅ websocket
✅ cache local
✅ mapping DTO → Entity

Datasource

Datasource:

remote
local
Remote Datasource
abstract class OrderRemoteDataSource {
  Future<List<OrderModel>> getOrders();
}
Implementasi Remote
class OrderRemoteDataSourceImpl
    implements OrderRemoteDataSource {

  final Dio dio;

  OrderRemoteDataSourceImpl(this.dio);

  @override
  Future<List<OrderModel>> getOrders() async {
    final response = await dio.get('/orders');

    return (response.data as List)
        .map((e) => OrderModel.fromJson(e))
        .toList();
  }
}
Model / DTO

Model khusus parsing JSON.

Contoh DTO
class OrderModel {
  final String id;
  final String orderNumber;

  OrderModel({
    required this.id,
    required this.orderNumber,
  });

  factory OrderModel.fromJson(Map<String, dynamic> json) {
    return OrderModel(
      id: json['id'],
      orderNumber: json['order_number'],
    );
  }
}
Mapper

DTO → Entity

Contoh Mapper
extension OrderMapper on OrderModel {
  OrderEntity toEntity() {
    return OrderEntity(
      id: id,
      orderNumber: orderNumber,
      total: 0,
    );
  }
}
Repository Implementation
class OrderRepositoryImpl
    implements OrderRepository {

  final OrderRemoteDataSource remote;

  OrderRepositoryImpl(this.remote);

  @override
  Future<List<OrderEntity>> getOrders() async {
    final result = await remote.getOrders();

    return result.map((e) => e.toEntity()).toList();
  }
}
FULL FLOW CLEAN ARCHITECTURE
UI
↓
Provider
↓
UseCase
↓
Repository Contract
↓
Repository Impl
↓
Datasource
↓
API
FLOW CREATE ORDER APOTIK
Pelayan Input Order
↓
OrderPage
↓
OrderNotifier
↓
CreateOrderUseCase
↓
OrderRepository
↓
OrderRepositoryImpl
↓
OrderRemoteDatasource
↓
POST /orders
↓
Backend Node.js
↓
PostgreSQL
FLOW REALTIME STOK
Kasir Bayar
↓
Backend update stock
↓
Emit websocket
↓
Flutter SocketService
↓
Realtime Provider
↓
UI auto refresh
REALTIME ARCHITECTURE
core/
└── websocket/
    ├── socket_service.dart
    ├── socket_listener.dart
    ├── socket_events.dart
    └── realtime_sync_service.dart
Socket Service
class SocketService {
  late IO.Socket socket;

  void connect() {
    socket = IO.io(
      baseUrl,
      IO.OptionBuilder()
          .setTransports(['websocket'])
          .build(),
    );
  }
}
Realtime State Update
socket.on('stock.updated', (data) {
  ref.read(stockProvider.notifier)
      .updateStock(data);
});
OFFLINE FIRST ARCHITECTURE

Untuk pelayan apotik.

Karena internet kadang tidak stabil.

Struktur Offline
core/offline/
├── sync_manager.dart
├── offline_queue.dart
├── pending_actions.dart
└── local_cache.dart
Flow Offline
Input Order
↓
Save Local Queue
↓
Internet Available
↓
Sync Background
↓
Server
Dependency Injection

Gunakan:

get_it
injectable
Struktur DI
core/dependency_injection/
├── injection.dart
├── modules/
└── injection.config.dart
Contoh DI
getIt.registerLazySingleton<OrderRepository>(
  () => OrderRepositoryImpl(
    getIt(),
  ),
);
Error Handling Architecture
Global Failure
abstract class Failure {}

class ServerFailure extends Failure {}

class NetworkFailure extends Failure {}
Result Pattern

Gunakan Either.

Future<Either<Failure, List<OrderEntity>>>
Base Response
class ApiResponse<T> {
  final bool success;
  final T? data;
  final String? message;
}
Folder Shared

Reusable component.

shared/
├── components/
├── dialogs/
├── forms/
├── tables/
└── layouts/
Multi Module Architecture

Untuk enterprise besar.

Contoh Module
features/
├── auth/
├── order/
├── inventory/
├── cashier/
├── report/
└── stock/
Modular Team Scaling

Tim bisa dipisah:

Tim	Modul
Team A	Order
Team B	Inventory
Team C	Report
Team D	Payment
Security Architecture
Token

Gunakan:

access token
refresh token
Secure Storage
FlutterSecureStorage
Network Layer Enterprise
core/network/
├── dio_client.dart
├── interceptors/
├── api_endpoints.dart
└── network_checker.dart
Interceptor
dio.interceptors.add(AuthInterceptor());
Logging Architecture
core/logger/
├── app_logger.dart
└── crash_reporter.dart
Analytics Architecture
core/analytics/
├── analytics_service.dart
└── event_tracker.dart
Clean Architecture Rules
Domain Layer

TIDAK BOLEH:

import Flutter
import Dio
import SQLite
Presentation Layer

TIDAK BOLEH:

query database langsung
hit API langsung
Data Layer

TIDAK BOLEH:

business logic berat
Testing Strategy
Unit Test

Test:

usecase
repository
mapper
Widget Test

Test:

page
widget
Integration Test

Test:

login
create order
payment
Best Practice Enterprise
Wajib

✅ feature-first
✅ modular
✅ repository pattern
✅ usecase pattern
✅ DTO terpisah entity
✅ realtime abstraction
✅ offline abstraction
✅ global error handling
✅ dependency injection
✅ centralized routing

Hindari

❌ business logic di widget
❌ API call di UI
❌ entity = model
❌ setState besar
❌ provider campur semua fitur
❌ folder utils monster

Arsitektur Final yang Direkomendasikan
Flutter UI
↓
Riverpod
↓
UseCase
↓
Repository Contract
↓
Repository Impl
↓
Datasource
↓
API / Local DB / WebSocket
Hasil Akhir

Dengan arsitektur ini aplikasi akan:

✅ enterprise ready
✅ scalable
✅ realtime ready
✅ offline ready
✅ multi platform
✅ mudah maintenance
✅ mudah testing
✅ mudah onboarding developer
✅ siap banyak cabang
✅ siap ribuan transaksi
✅ production ready
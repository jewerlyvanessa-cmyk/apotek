Tujuan Realtime Stock

Sistem harus memastikan:

✅ stok selalu akurat
✅ tidak overselling
✅ realtime antar device
✅ realtime antar cabang
✅ support banyak user bersamaan
✅ scalable
✅ aman dari race condition

Masalah yang Harus Diselesaikan

Contoh kasus:

Stok Paracetamol = 5

Pelayan A pesan 3
Pelayan B pesan 4

Jika tidak realtime + locking:
stok bisa minus

Karena itu dibutuhkan:

stock reservation
database transaction
row locking
websocket broadcast
optimistic UI
Arsitektur Realtime Stock Enterprise
Flutter App
↓
WebSocket Client
↓
Node.js Gateway
↓
Redis Pub/Sub
↓
Inventory Service
↓
PostgreSQL
Teknologi Realtime yang Direkomendasikan
Flutter
socket_io_client
Backend Node.js

Rekomendasi:

NestJS + WebSocket Gateway
ATAU
Fastify + Socket.IO
Redis

Dipakai untuk:

websocket scaling
pub/sub event
distributed cache
stock sync
Flow Realtime Lengkap
FLOW 1 — Pelayan Input Order
Pelayan Cari Obat
↓
Flutter request stock realtime
↓
Backend cek stock available
↓
Tampilkan stock realtime
FLOW 2 — Reserve Stock

Paling penting.

Saat pelayan submit order:

START TRANSACTION
↓
Lock row stock
↓
Cek available stock
↓
Tambah reserved_quantity
↓
Create order
↓
COMMIT
↓
Broadcast websocket
Formula Stock
available_stock =
quantity - reserved_quantity
Contoh
quantity = 10
reserved = 3

available = 7
PostgreSQL Table
stocks
---------
id
branch_id
medicine_id
quantity
reserved_quantity
updated_at
SQL Locking Enterprise

WAJIB pakai:

SELECT *
FROM stocks
WHERE medicine_id = $1
FOR UPDATE;

Ini mencegah race condition.

Kenapa FOR UPDATE Penting?

Tanpa lock:

User A baca stok = 5
User B baca stok = 5

dua-duanya berhasil beli
stok jadi minus

Dengan lock:

request kedua menunggu
data aman
Flow Detail Reserve Stock
Pelayan submit order
↓
Backend transaction begin
↓
Lock stock row
↓
Check available stock
↓
Reserve stock
↓
Create order
↓
Commit
↓
Emit websocket
Event WebSocket
stock.updated
{
  "medicine_id": "uuid",
  "branch_id": "uuid",
  "quantity": 20,
  "reserved_quantity": 5,
  "available_quantity": 15
}
Flutter Listen Event
socket.on('stock.updated', (data) {
  ref.read(stockProvider.notifier)
      .updateRealtimeStock(data);
});
UI Auto Update

Saat websocket masuk:

Kasir
↓
Stock berubah otomatis

Pelayan
↓
Stock berubah otomatis

Admin Gudang
↓
Stock berubah otomatis

Tanpa refresh manual.

FLOW 3 — Payment Success

Saat kasir bayar:

START TRANSACTION
↓
Lock stock
↓
Kurangi quantity
↓
Kurangi reserved_quantity
↓
Update order status
↓
Insert payment
↓
Insert stock movement
↓
COMMIT
↓
Broadcast websocket
Contoh

Sebelum:

quantity = 10
reserved = 3

Setelah bayar 3:

quantity = 7
reserved = 0
FLOW 4 — Order Cancel
Cancel Order
↓
Release reserved stock
↓
Broadcast websocket
PostgreSQL Transaction

WAJIB.

Contoh:

BEGIN;

SELECT * FROM stocks
WHERE id = $1
FOR UPDATE;

UPDATE stocks
SET reserved_quantity = reserved_quantity + 2
WHERE id = $1;

COMMIT;
Realtime Multi Device

Contoh:

Pelayan HP Android
Kasir iPad
Owner Desktop
Gudang Laptop

Semua sinkron realtime.

Realtime Multi Branch

Socket room:

tenant:1
branch:2
Contoh Join Room
socket.join(`tenant:${tenantId}`);
socket.join(`branch:${branchId}`);
Broadcast Specific Branch
io.to(`branch:${branchId}`)
  .emit('stock.updated', payload);
Arsitektur Redis Scaling

Untuk banyak server.

Socket Server A
↓
Redis Adapter
↓
Socket Server B
↓
Socket Server C
Kenapa Redis Penting?

Tanpa Redis:

websocket hanya lokal server

Dengan Redis:

realtime lintas server
Event Realtime yang Wajib
inventory
stock.updated
stock.low
stock.expired
stock.transfer
order
order.created
order.updated
order.cancelled
cashier
payment.success
payment.failed
Struktur Backend Realtime
src/
├── websocket/
│   ├── gateways/
│   ├── events/
│   ├── rooms/
│   ├── adapters/
│   └── handlers/
Struktur Flutter Realtime
core/websocket/
├── socket_service.dart
├── socket_manager.dart
├── socket_events.dart
├── realtime_dispatcher.dart
└── realtime_handlers/
Realtime Handler Pattern
abstract class SocketHandler {
  void handle(dynamic payload);
}
Contoh Handler
class StockUpdatedHandler
    implements SocketHandler {

  @override
  void handle(payload) {
    // update provider
  }
}
Offline + Realtime Strategy

Untuk apotik sangat penting.

Saat Offline
Input order
↓
Save local queue
↓
Auto sync ketika online
Conflict Resolution

Jika stok berubah saat offline:

Server jadi source of truth
Flow Sync Offline
Offline Order
↓
Pending Queue
↓
Reconnect
↓
Validate stock ulang
↓
Sync
Optimistic UI

UI langsung terasa cepat.

Contoh

Pelayan klik tambah qty:

UI langsung berubah
↓
Server validate
↓
Jika gagal rollback UI
Stock Movement Ledger

JANGAN update stok tanpa histori.

WAJIB ada:

stock_movements
Tabel Stock Movement
movement_type:
- SALE
- PURCHASE
- ADJUSTMENT
- TRANSFER
- CANCEL
- RETURN
Auditability

Semua perubahan stok harus bisa dilacak.

Performance Strategy
Gunakan Cache Redis
Hot stock cache
Gunakan Batch Event

Jangan spam websocket.

Gunakan Debounce

Untuk pencarian obat realtime.

Gunakan Index PostgreSQL
CREATE INDEX idx_stock_branch_medicine
ON stocks(branch_id, medicine_id);
Gunakan Partition

Untuk:

orders
stock_movements
audit_logs
Enterprise Safety

WAJIB:

✅ transaction
✅ row locking
✅ websocket retry
✅ reconnect strategy
✅ heartbeat ping
✅ audit log
✅ stock ledger
✅ reservation stock
✅ rollback handling

Hindari

❌ update stok langsung tanpa reserve
❌ websocket tanpa reconnect
❌ polling tiap detik
❌ tanpa database lock
❌ stok dihitung di frontend
❌ tanpa transaction

Arsitektur Realtime Final
Flutter
↓
Riverpod State
↓
Socket Service
↓
Node.js Gateway
↓
Redis Pub/Sub
↓
Inventory Service
↓
PostgreSQL Transaction + Lock
↓
Broadcast Realtime
↓
All Device Updated
Hasil Akhir

Dengan flow ini aplikasi Anda akan:

✅ realtime antar device
✅ aman dari overselling
✅ scalable multi server
✅ cocok banyak cabang
✅ support ribuan transaksi
✅ sinkron stok realtime
✅ enterprise-grade inventory system
✅ production-ready untuk jaringan apotik besar